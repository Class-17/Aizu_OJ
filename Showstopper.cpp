#include <cstdio>
#include <cstring>
typedef long long ll;

const int maxn = 1e5;

int x[maxn], y[maxn], z[maxn];

int main() {
    char s[50] = "\0";
    int n = 0;
    while (scanf("%[^\n]*c", s) != EOF) {
        if (s[0] == '\0') {
            getchar();
            continue;
        }
        int len = strlen(s);
        int i = 0;
        x[n] = y[n] = z[n] = 0;
        
        
        s[0] = '\0';
    }    
    return 0;
}